# MongoDB Atlas connection string (same as backend)
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<dbname>?retryWrites=true&w=majority

# Admin panel login credentials
ADMIN_EMAIL=admin@furniro.com
ADMIN_PASSWORD=your_secure_admin_password

# Admin panel port
ADMIN_PORT=3002

# Cookie secret — use a strong random string (32+ characters)
COOKIE_SECRET=change-this-to-a-very-long-random-string-at-least-32-chars

# Session secret — use a strong random string (32+ characters)
SESSION_SECRET=change-this-to-another-long-random-string-at-least-32-chars

# Node Environment
NODE_ENV=production

# Resend Email Service
RESEND_API_KEY=re_your_api_key_here
# The sender address. Using onboarding@resend.dev lets you send to your registered account email.
# To send to arbitrary user emails, verify a custom domain in your Resend dashboard (resend.com/domains).
RESEND_FROM_EMAIL="Furniro <orders@praver.space>"
SUPPORT_EMAIL=tech.prav7@gmail.com
